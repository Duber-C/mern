function Loading() {
  return (
    <main
      style={{
        backgroundColor: "#363740",
        height: "100vh",
        display: "grid",
        alignItems: "center",
        justifyItems: "center",
        color: "gray",
        fontSize: "20px",
      }}
    >
      <div style={{ display: "grid", justifyItems: "center" }}>
        <p>Loading...</p>
      </div>
    </main>
  );
}

export default Loading;
